diff --git forkSrcPrefix/filters/f_decoder_wrapper.c forkDstPrefix/filters/f_decoder_wrapper.c
index 5e2e8d7..b3c4d5e 100644
--- forkSrcPrefix/filters/f_decoder_wrapper.c
+++ forkDstPrefix/filters/f_decoder_wrapper.c
@@ -193,6 +193,8 @@ struct priv {
     // Final PTS of previously decoded frame
     double pts;

+    bool drop_backward;
+
     struct mp_image_params dec_format, last_format, fixed_format;

     double fps;
@@ -792,6 +794,14 @@ static void correct_video_pts(struct priv *p, struct mp_image *mpi)
         }
     }

+    p->drop_backward = false;
+    if (p->pts != MP_NOPTS_VALUE && mpi->pts != MP_NOPTS_VALUE &&
+        mpi->pts < p->pts - 1.0)
+    {
+        p->drop_backward = true;
+        return;
+    }
+
     p->pts = mpi->pts;
 }

@@ -808,6 +818,7 @@ static void correct_audio_pts(struct priv *p, struct mp_aframe *aframe)

     double frame_pts = mp_aframe_get_pts(aframe);
     double frame_len = mp_aframe_duration(aframe);
+    p->drop_backward = false;

     if (frame_pts != MP_NOPTS_VALUE) {
         if (dir < 0)
@@ -816,7 +827,7 @@ static void correct_audio_pts(struct priv *p, struct mp_aframe *aframe)
         // than enough.
         if (p->pts != MP_NOPTS_VALUE && diff > 0.1) {
             MP_WARN(p, "Invalid audio PTS: %f -> %f\n", p->pts, frame_pts);
-            if (diff >= 5) {
+            if (diff >= 300) {
                 mp_mutex_lock(&p->cache_lock);
                 p->pts_reset = true;
                 mp_mutex_unlock(&p->cache_lock);
@@ -826,7 +837,12 @@ static void correct_audio_pts(struct priv *p, struct mp_aframe *aframe)
         // Keep the interpolated timestamp if it doesn't deviate more
         // than 1 ms from the real one. (MKV rounded timestamps.)
         if (p->pts == MP_NOPTS_VALUE || diff > 0.001)
-            p->pts = frame_pts;
+        {
+            if (p->pts != MP_NOPTS_VALUE && frame_pts < p->pts && diff > 0.1) {
+                p->drop_backward = true;
+                return;
+            }
+            p->pts = frame_pts;
+        }
     }

     if (p->pts == MP_NOPTS_VALUE && p->header->missing_timestamps)
@@ -1083,6 +1099,11 @@ static void read_frame(struct priv *p)

 output_frame:
     process_output_frame(p, frame);
+    if (p->drop_backward) {
+        mp_frame_unref(&frame);
+        mp_filter_internal_mark_progress(p->decf);
+        return;
+    }
     mp_pin_in_write(pin, frame);
 }

diff --git forkSrcPrefix/player/playloop.c forkDstPrefix/player/playloop.c
index f111920..5747f2d 100644
--- forkSrcPrefix/player/playloop.c
+++ forkDstPrefix/player/playloop.c
@@ -1123,16 +1123,17 @@ static void handle_dummy_ticks(struct MPContext *mpctx)
 // Update current playback time.
 static void handle_playback_time(struct MPContext *mpctx)
 {
+    double new_pts = MP_NOPTS_VALUE;
     if (mpctx->vo_chain &&
         !mpctx->vo_chain->is_sparse &&
         mpctx->video_status >= STATUS_PLAYING &&
         mpctx->video_status < STATUS_EOF)
     {
-        mpctx->playback_pts = mpctx->video_pts;
+        new_pts = mpctx->video_pts;
     } else if (mpctx->audio_status >= STATUS_PLAYING &&
                mpctx->audio_status < STATUS_EOF)
     {
-        mpctx->playback_pts = playing_audio_pts(mpctx);
+        new_pts = playing_audio_pts(mpctx);
     } else if (mpctx->video_status == STATUS_EOF &&
                mpctx->audio_status == STATUS_EOF)
     {
@@ -1140,7 +1141,14 @@ static void handle_playback_time(struct MPContext *mpctx)
         double vpts = mpctx->video_pts;
         double mpts = MP_PTS_MAX(apts, vpts);
         if (mpts != MP_NOPTS_VALUE)
-            mpctx->playback_pts = mpts;
+            new_pts = mpts;
+    }
+
+    if (new_pts != MP_NOPTS_VALUE) {
+        if (mpctx->playback_pts != MP_NOPTS_VALUE &&
+            new_pts < mpctx->playback_pts - 1.0)
+            return;
+        mpctx->playback_pts = new_pts;
     }
 }

